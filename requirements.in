# Core dependencies for ACE Framework
litellm>=1.78.0
python-dotenv>=1.0.0
pydantic>=2.0.0
tenacity>=8.0.0

# Demo dependencies
rich>=13.0.0

# Benchmark integration dependencies
datasets>=2.0.0
pyyaml>=6.0.0

# Optional for local model support (uncomment if needed)
# transformers>=4.30.0
# torch>=2.0.0
# accelerate>=0.20.0

# Optional benchmark dependencies (install separately if needed)
# appworld>=0.1.0  # For AppWorld benchmark support

# Database dependencies for ACE Ticket learning
# Note: pgvector is a PostgreSQL server-side extension, not a Python package
# Install it on the PostgreSQL server: CREATE EXTENSION vector;
pymysql>=1.0.0
pg8000>=1.30.0
psycopg2-binary>=2.9.0
sqlalchemy>=2.0.0

# Additional dependencies for ACE Ticket
httpx>=0.25.0
markdownify>=0.11.0